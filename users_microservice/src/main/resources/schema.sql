CREATE TABLE user (
                      id varchar NOT NULL,
                      password varchar NOT NULL,
                      firstName varchar,
                      lastName varchar,
                      userType varchar,
                      PRIMARY KEY (id)
);